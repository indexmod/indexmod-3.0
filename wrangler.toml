# wrangler.toml — конфигурация воркера

name = "indexmod-worker"       # имя воркера, подставляется в URL
type = "javascript"               # тип скрипта
compatibility_date = "2025-12-01" # дата совместимости

# Путь к статическим файлам (если есть)
# assets = { directory = "./dist" }

# Пример окружений (опционально)
[env.production]
# Тут можно переопределять секреты и переменные для prod
